# Overseerr Service
PUID=1000          # Your user ID (use `id -u` on Linux)
PGID=1000          # Your group ID (use `id -g` on Linux)
TZ=America/Toronto # Set to your timezone (e.g., "Europe/London")

# Tailscale Authentication
TS_AUTHKEY=your-tailscale-auth-key  # Generate from Tailscale admin panel

# Docker Volume Base Path
DOCKER_VOLUME_BASE=/absolute/path/to/docker/volumes  # Set an absolute path
